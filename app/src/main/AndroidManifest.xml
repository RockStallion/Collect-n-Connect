<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Collectnconnect"
        tools:targetApi="31">
        <activity
            android:name="com.collect.connect.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Collectnconnect">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.collect.connect.RegisterActivity" />
        <activity android:name="com.collect.connect.Principal" />
        <activity android:name="com.collect.connect.Collections" />
        <activity android:name="com.collect.connect.Pieces" />
        <activity android:name="com.collect.connect.Scan" />
        <activity android:name="com.collect.connect.Sets" />
        <activity android:name="com.collect.connect.AgregarSetActivity" />
        <activity android:name="com.collect.connect.Perfil" />

    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="true"/>
</manifest>